/**
 * Keep string constants in one place so that they can be changed.
 */
export const PATHS = {
  petProfile: (id: string) => `/adoptable-dogs-cats/pet/${id}`,
  search: () => "/adoptable-dogs-cats",
};
