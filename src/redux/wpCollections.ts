import {createSlice} from "@reduxjs/toolkit";


/**
 * want to key by number instead of page in order to support combining multiple limits
 */

export const wpCollectionsSlice = createSlice({
    name: 'wpCollections',
    initialState: {},
    reducers: {

    }
})
