export interface WpAuthorJson {
    id: number;
    name: string;
    url: string; // this is for the author's personal url; so almost always empty
    description: string;
    link: string;
    slug: string;
}
